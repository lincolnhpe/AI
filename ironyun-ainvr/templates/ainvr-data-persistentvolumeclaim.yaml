apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    com.ironyun.service: ainvr-data-pvc
  name: ainvr-data-pvc
spec:
  storageClassName: nfs-csi
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
