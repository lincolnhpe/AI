apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    com.ironyun.service: ainvr-secrets-pvc
  name: ainvr-secrets-pvc
spec:
  storageClassName: nfs-csi
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
