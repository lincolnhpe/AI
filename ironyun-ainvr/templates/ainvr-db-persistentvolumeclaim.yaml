apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    com.ironyun.service: ainvr-db-pvc
  name: ainvr-db-pvc
spec:
  storageClassName: nfs-csi
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
status: {}
